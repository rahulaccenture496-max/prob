
  # Convert to Landing Page

  This is a code bundle for Convert to Landing Page. The original project is available at https://www.figma.com/design/BuZ6Fs1sTQYbn4Z7kFDPqJ/Convert-to-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  